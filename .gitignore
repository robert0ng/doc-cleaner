# Credentials — never commit these
.env
secrets.json
config.json
credentials.json
token.json

# Output files (keep the module code: output/__init__.py, output/markdown.py)
output/*.md
output/*/
test-output/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# macOS
.DS_Store

# Claude Code / Spectra (local dev config, not part of the project)
.claude/
.spectra/
openspec/
CLAUDE.md
.claude-memory
opencode.json
